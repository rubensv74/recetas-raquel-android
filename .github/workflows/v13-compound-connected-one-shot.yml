name: V13 compound connected one-shot gate

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pending-catalog-update:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while the editorial v13 batch is applied."
