name: V13 instrumented gate (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Gate 55: hosted Android emulator did not boot reliably."
          echo "Run connectedDebugAndroidTest on a stable local AVD before releasing v13."
          exit 1
